import React from 'react';
import styles from '../Styles/Estilos.module.css';

const Header = () => {
    return (
        <nav className={styles.myNav}>
            <p>My Youtube Player</p>
        </nav>
        );
}

export default Header
